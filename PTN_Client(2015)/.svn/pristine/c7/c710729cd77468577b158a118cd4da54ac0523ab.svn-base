package com.nms.db.dao.alarm;

import java.util.List;

import org.apache.ibatis.annotations.Param;

import com.nms.db.bean.alarm.CurrentAlarmInfo;


public interface CurrentAlarmInfoMapper {
	public List<CurrentAlarmInfo> queryByCondition(CurrentAlarmInfo currAlarm);

	public void update(CurrentAlarmInfo curr);

	/**
	 * 搜索时，更新告警
	 * @param newTunnelId
	 * @param tid
	 * @param i
	 */
	public void updateObjectid(int newTunnelId, String tid, int i);
	
	/**
	 * 关联查询 关联site和warninglevel表
	 * @param currentAlarmInfo
	 * @return
	 */
	public List<CurrentAlarmInfo> query_join(@Param("currentAlarm")CurrentAlarmInfo currentAlarmInfo);
	
	/**
	 * 通过槽位查询当前告警
	 * @param slotInst	槽位
	 */
	public List<CurrentAlarmInfo> queryByClent();
	/**
	 * 删除当前告警数据
	 * @param curId
	 *            当前告警主键id
	 * @return 插入的记录数
	 */
	public int delete(@Param("curId")Integer curId);
}